<!-- Lucide "x" (ISC licence, lucide.dev). 24x24, 2px stroke, round caps.
     Kept here as the source of truth; js/theme.js draws the same geometry with
     canvas paths so it can be tinted and scaled without an image load. -->
<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"
     fill="none" stroke="currentColor" stroke-width="2"
     stroke-linecap="round" stroke-linejoin="round">
  <path d="M18 6 6 18"/>
  <path d="m6 6 12 12"/>
</svg>
